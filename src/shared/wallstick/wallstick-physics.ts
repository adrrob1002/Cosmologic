import { Workspace } from "@rbxts/services";

export class WallstickPhysics {
	public updateGyro() {
		const cameraCFrame = Workspace.CurrentCamera?.CFrame;
		const isRelative = Workspace.CurrentCamera?.FindFirstChild;
	}
}
